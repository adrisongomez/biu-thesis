mode: deployment

service:
  enabled: true
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8888"

resources:
  limits:
    cpu: 100m
    memory: 200M

config:
  exporters:
    otlp/custom:
      endpoint: "custom-collector.custom-collector.svc.cluster.local:4317"
      tls:
        insecure: true
    
  service:
    pipelines:
      traces:
        receivers: [otlp]
        processors: [batch]
        exporters: [otlp/custom]
      metrics:
        receivers: [otlp]
        processors: [batch]